#jinja2: trim_blocks: True, lstrip_blocks: True
{{ ansible_managed | comment }}

# =============================================================================
# NFTables Main Configuration File
# =============================================================================
# This is the main configuration file for NFTables firewall.
# It serves as the entry point that includes all rule files from the 
# rules directory. The firewall rules are loaded in alphabetical order
# based on their filenames, which typically include numeric prefixes to
# control execution order.
#
# Generated by Ansible - Manual changes will be overwritten
# =============================================================================

#!/usr/sbin/nft -f

# Include all rule files from the NFTables rules directory
# This uses the glob pattern to load all files with the .rules extension
include "{{ nftables_rules_directory_path }}/*.rules"
